# 说明：
# - 本仓库约定：只有「敏感信息」放在 .env 中（邮箱 / API Key / token / cookie / license 等）。
# - 其它非敏感配置（端口、路径、开关、模型名等）已直接写在 docker-compose.yml 里。

# ---- SiliconFlow (敏感) ----
SILICONFLOW_API_KEY=

# ---- WeChat2RSS (敏感) ----
LIC_EMAIL=
LIC_CODE=

# ---- RSSHub (敏感) ----
ZSXQ_ACCESS_TOKEN=
SEEKINGALPHA_COOKIE=
TWITTER_AUTH_TOKEN_COOKIE=
